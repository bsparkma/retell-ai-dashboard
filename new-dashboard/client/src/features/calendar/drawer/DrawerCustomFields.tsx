"use client";

/** Phase 1: placeholder. Custom fields come from ApptFieldDefs + ApptFields in a later phase. */
export function DrawerCustomFields() {
  return (
    <section className="space-y-2">
      <h4 className="text-sm font-semibold text-foreground">Custom fields</h4>
      <p className="text-sm text-muted-foreground">No custom fields</p>
    </section>
  );
}
